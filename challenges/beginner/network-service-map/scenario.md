# Scenario

You need to map what the analyst container can reach inside the isolated training network.

## Objective

Create a small service inventory for local lab targets only.

## Tasks

1. Resolve the `web` service name.
2. Check HTTP availability for `web:8080`.
3. Scan only the `web` service inside the lab network.
4. Explain why scanning external systems is outside scope.

