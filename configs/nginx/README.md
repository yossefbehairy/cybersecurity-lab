# Nginx Config Placeholder

Nginx is intentionally not part of v1 runtime. The Flask app binds directly to localhost through Docker port publishing to keep the lab lightweight.

Use this directory for a future optional reverse-proxy profile.

