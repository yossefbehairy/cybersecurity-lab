# Monitoring Config

v1 monitoring is intentionally minimal and uses health endpoints plus structured event queries. Heavy stacks such as Elastic, Kibana, and Wazuh are intentionally excluded from the default profile.

